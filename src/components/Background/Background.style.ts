import styled from 'styled-components';

export const svg = styled.svg`
    background-color: rgb(233, 219, 109);
    height: 100vh;
    width: 100vw;
    filter: opacity(0.7);
    position: absolute;
    background-attachment: fixed;
`;
